import styled from 'styled-components';

export const Container = styled.div`
    opacity: 100%;
    .progressBar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    height: .5rem;
    background:var(--wht);
    transform-origin: 0%;
}

`;